import { StyleSheet } from 'react-native'

export default StyleSheet.create({
  headerRight: {
    flex: 1,
    marginRight: 8,
    paddingHorizontal: 8,
    alignItems: 'center',
    justifyContent: 'center'
  }
})
